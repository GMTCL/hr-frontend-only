# Frontend environment variables for production
# Copy this file to .env.production and update the values

# Base URL of Laravel API (include /api)
NEXT_PUBLIC_API_URL=https://hrbackoofice.mimshack-sourc.com/api

# Optional: force locale (usually controlled by next-intl middleware)
# NEXT_PUBLIC_DEFAULT_LOCALE=th

# Optional: enable debug mode (should be false in production)
# NEXT_PUBLIC_DEBUG=false
